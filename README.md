# BGG OWL Trade Helper

A browser extension to help manage OLWLG BGG Math trade lists.

## Current features

### Add money items over a given value

When on the PayPal view, you can enter a value and add all entries greater than or equal to that value.

### Set values to money entries

When on step 4, you can automatically set the values for monetary entries you've selected.
